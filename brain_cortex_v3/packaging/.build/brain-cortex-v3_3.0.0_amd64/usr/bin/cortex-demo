#!/usr/bin/env sh
# Brain Cortex V3 — Live Advice Grid Demo
set -eu
[ -f /etc/default/brain-cortex-v3 ] && . /etc/default/brain-cortex-v3
cd /usr/lib/brain-cortex-v3
exec "${BCX_PYTHON:-python3}" cortex_game.py --demo --frames "${1:-50}"
